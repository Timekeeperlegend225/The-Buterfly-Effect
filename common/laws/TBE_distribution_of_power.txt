# group = this is the law_group a law belongs to
# icon = graphical icon shown in-game
# modifier = {} modifier on country for having adopted this law

# Categories for ideological stance

REPLACE_OR_CREATE:law_aristocratic = { #Traditional
	group = lawgroup_distribution_of_power

	icon = "gfx/interface/icons/law_icons/landed_voting.dds"

	progressiveness = -50

	is_visible = {
		always = no
	}

	modifier = {
		country_aristocrats_pol_str_mult = 0.5
		country_legitimacy_base_add = 100
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_institutional = { #Institutional
	group = lawgroup_distribution_of_power

	icon = "gfx/interface/icons/law_icons/presidential_republic.dds"

	progressiveness = 0

	is_visible = {
		always = no
	}

	unlocking_technologies = {
		centralization
	}

	modifier = {
		country_legitimacy_base_add = 100
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_plutocratic = { #Wealth
	group = lawgroup_distribution_of_power

	icon = "gfx/interface/icons/law_icons/wealth_voting.dds"

	progressiveness = 0

	is_visible = {
		always = no
	}

	unlocking_technologies = {
		currency_standards
	}

	modifier = {
		state_political_strength_from_wealth_mult = 1
		country_legitimacy_base_add = 100
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_meritocratic = { #Qualification
	group = lawgroup_distribution_of_power

	icon = "gfx/interface/icons/law_icons/census_voting.dds"

	progressiveness = 50

	is_visible = {
		always = no
	}

	unlocking_technologies = {
		empiricism
	}

	modifier = {
		country_legitimacy_base_add = 100
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_egalitarian = { #Universal
	group = lawgroup_distribution_of_power

	icon = "gfx/interface/icons/law_icons/universal_suffrage.dds"

	progressiveness = 100

	is_visible = {
		always = no
	}

	unlocking_technologies = {
		egalitarianism
	}

	modifier = {
		country_legitimacy_base_add = 100
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_partisan = { #Controlled
	group = lawgroup_distribution_of_power

	icon = "gfx/interface/icons/law_icons/single_party_state.dds"

	progressiveness = 0

	is_visible = {
		always = no
	}

	unlocking_technologies = {
		political_agitation
	}

	modifier = {
		country_legitimacy_base_add = 100
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
	}

	ai_impose_chance = {
		value = 0
	}
}

# Autocratic

REPLACE_OR_CREATE:law_divine_right = {
	group = lawgroup_distribution_of_power
	parent = law_aristocratic

	icon = "gfx/interface/icons/law_icons/state_religion.dds"

	progressiveness = -100

	is_visible = {
		has_law = law_type:law_autocracy
	}

	modifier = {
		country_authority_add = 50
		country_legitimacy_headofstate_add = 30
		country_legitimacy_govt_total_clout_add = 70
		country_aristocrats_pol_str_mult = 1
		country_clergymen_pol_str_mult = 1
	}
	
	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_autocracy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_loyalism = {
	group = lawgroup_distribution_of_power
	parent = law_institutional

	icon = "gfx/interface/icons/law_icons/autocracy.dds"

	progressiveness = -100

	unlocking_technologies = {
		centralization
	}

	is_visible = {
		has_law = law_type:law_autocracy
	}

	modifier = {
		country_authority_add = 100
		country_legitimacy_headofstate_add = 30
		country_legitimacy_govt_total_clout_add = 60
	}
	
	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_autocracy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_clientelism = {
	group = lawgroup_distribution_of_power
	parent = law_plutocratic

	icon = "gfx/interface/icons/law_icons/per_capita_based_taxation.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_autocracy
	}

	unlocking_technologies = {
		currency_standards
	}

	modifier = {
		country_legitimacy_headofstate_add = 20
		country_legitimacy_govt_total_clout_add = 100
		state_political_strength_from_wealth_mult = 1
	}
	
	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_autocracy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_enlightened_despotate = {
	group = lawgroup_distribution_of_power
	parent = law_meritocratic

	icon = "gfx/interface/icons/law_icons/neo_absolutism.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_autocracy
	}

	unlocking_technologies = {
		empiricism
	}

	modifier = {
		country_authority_add = 50
		country_legitimacy_headofstate_add = 20
		country_legitimacy_govt_total_clout_add = 70
		country_academics_pol_str_mult = 1
		country_bureaucrats_pol_str_mult = 0.5
		country_engineers_pol_str_mult = 0.2
		country_agitator_slots_add = 1
	}
	
	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_autocracy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_populism = {
	group = lawgroup_distribution_of_power
	parent = law_egalitarian

	icon = "gfx/interface/icons/law_icons/organic_regulation.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_autocracy
	}

	unlocking_technologies = {
		egalitarianism
	}

	modifier = {
		country_authority_add = 50
		country_legitimacy_headofstate_add = 30
		country_legitimacy_govt_total_clout_add = 40
		country_bolster_attraction_factor = 0.20
		political_movement_pop_attraction_mult = 0.30
		political_movement_radicalism_add = 0.20
		political_movement_radicalism_from_enactment_approval_mult = 0.20
		state_loyalists_from_political_movements_mult = 1
		political_movement_radicalism_from_enactment_disapproval_mult = 0.20
	}
	
	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_autocracy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_personality_rule = {
	group = lawgroup_distribution_of_power
	parent = law_partisan

	icon = "gfx/interface/icons/law_icons/secret_police.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_autocracy
	}

	unlocking_technologies = {
		political_agitation
	}

	modifier = {
		country_authority_add = 150
		country_legitimacy_headofstate_add = 20
		country_legitimacy_govt_total_clout_add = 40
		country_legitimacy_govt_size_add = 1
		country_legitimacy_ideological_incoherence_mult = 0.5
		interest_group_in_government_attraction_mult = 0.10
	}
	
	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_autocracy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_aristocracy = {
	group = lawgroup_distribution_of_power
	parent = law_aristocratic

	icon = "gfx/interface/icons/law_icons/landed_voting.dds"

	progressiveness = -100

	is_visible = {
		has_law = law_type:law_oligarchy
	}

	modifier = {
		country_legitimacy_govt_total_clout_add = 100
		country_legitimacy_ideological_incoherence_mult = 0.20
		country_legitimacy_govt_size_add = 1
		country_aristocrats_pol_str_mult = 2
		country_clergymen_pol_str_mult = 1
		interest_group_ig_landowners_pol_str_mult = 0.10
		country_authority_add = 100
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_oligarchy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
				scope:target_country = {
					NOR = { # Is their law different enough from what we want to impose?
						has_law_or_variant = law_type:law_autocracy
						has_law_or_variant = law_type:law_aristocratic
					}
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_institutional_rule = {
	group = lawgroup_distribution_of_power
	parent = law_institutional

	icon = "gfx/interface/icons/law_icons/presidential_republic.dds"

	progressiveness = -100

	is_visible = {
		has_law = law_type:law_oligarchy
	}

	modifier = {
		country_legitimacy_govt_total_clout_add = 100
		country_authority_add = 100
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_oligarchy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
				scope:target_country = {
					NOR = { # Is their law different enough from what we want to impose?
						has_law_or_variant = law_type:law_autocracy
						has_law_or_variant = law_type:law_aristocratic
					}
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_plutocracy = {
	group = lawgroup_distribution_of_power
	parent = law_plutocratic

	icon = "gfx/interface/icons/law_icons/wealth_voting.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_oligarchy
	}

	modifier = {
		country_legitimacy_govt_total_clout_add = 100
		country_legitimacy_ideological_incoherence_mult = -0.2
		country_legitimacy_govt_size_add = 1
		country_aristocrats_pol_str_mult = 0.5
		country_capitalists_pol_str_mult = 1
		state_political_strength_from_wealth_mult = 1
		country_authority_add = 100
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_oligarchy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
				scope:target_country = {
					NOR = { # Is their law different enough from what we want to impose?
						has_law_or_variant = law_type:law_autocracy
						has_law_or_variant = law_type:law_aristocratic
					}
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE:law_technocracy = {
	group = lawgroup_distribution_of_power
	parent = law_meritocratic

	icon = "gfx/interface/icons/law_icons/technocracy.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_oligarchy
	}

	unlocking_technologies = {
		empiricism
	}

	modifier = {
		country_legitimacy_govt_total_clout_add = 100
		country_legitimacy_ideological_incoherence_mult = -0.2
		country_legitimacy_govt_size_add = 2
		country_academics_pol_str_mult = 1
		country_engineers_pol_str_mult = 1
		country_bureaucrats_pol_str_mult = 0.5
		country_officers_pol_str_mult = 0.50
		country_authority_add = 100
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_technocracy
				"ai_ideological_opinion(scope:target_country)" >= 25 # Only want to do this on ideologically similar countries
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_partisan }
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE:law_anarchy = {
	group = lawgroup_distribution_of_power
	parent = law_egalitarian

	icon = "gfx/interface/icons/law_icons/anarchy.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_oligarchy
	}

	unlocking_technologies = {
		egalitarianism
	}

	modifier = {
		state_pop_pol_str_add = 1
		country_legitimacy_govt_total_clout_add = 100
		country_legitimacy_ideological_incoherence_mult = -0.5
		country_legitimacy_govt_size_add = 3
	}

	can_enact = {
		trigger_if = {
			limit = {
				c:BIC ?= ROOT
				is_subject = yes
				NOT = { has_law = scope:law }
			}
			custom_tooltip = {
				text = BIC_not_able_to_change_distribution_of_power_tt
				has_variable = britain_granted_dop_permission
			}
		}
	}

	on_activate = {
		scripted_effect_parties_disappearence = yes
		cleanup_voting_strength_modifiers = yes
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_anarchist
		}
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			AND = { # modification of the standard trigger, only other anarchic countries can impose anarchy on their subjects
				has_law = scope:law
				can_impose_law_default = yes
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_anarchy
				has_strategy = ai_strategy_egalitarian_agenda
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_egalitarian }
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_ideological_elite = {
	group = lawgroup_distribution_of_power
	parent = law_partisan

	icon = "gfx/interface/icons/law_icons/single_party_state.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_oligarchy
	}

	unlocking_technologies = {
		political_agitation
	}

	modifier = {
		country_authority_add = 100
		country_legitimacy_govt_total_clout_add = 100
		interest_group_in_government_attraction_mult = 0.20
		country_legitimacy_govt_size_add = 2
		country_bolster_attraction_factor = 0.25
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			interest_group = {
				has_party = yes
			}
			law_stance = {
				law = law_type:law_partisan
				value > neutral
			}
		}
	}
	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			AND = { # modification of the standard trigger, only other single-party states countries can impose single-party states on their subjects
				has_law = scope:law
				can_impose_law_default = yes
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_partisan
				"ai_ideological_opinion(scope:target_country)" >= 25 # Only want to do this on ideologically similar countries
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_technocracy }
				}
			}

			add = base_impose_law_weight
		}
	}
}

# Concilliary

REPLACE_OR_CREATE:law_noble_council = {
	group = lawgroup_distribution_of_power
	parent = law_aristocratic

	icon = "gfx/interface/icons/law_icons/oligarchy.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_counciliary
	}

	modifier = {
		country_authority_add = 50
		country_legitimacy_govt_total_votes_add = 40
		country_legitimacy_govt_total_clout_add = 80
		country_aristocrats_voting_power_add = 50
		country_aristocrats_pol_str_mult = 0.5
		country_clergymen_voting_power_add = 30
		country_clergymen_pol_str_mult = 0.2
		country_officers_voting_power_add = 20
		country_officers_pol_str_mult = 0.1
		country_capitalists_voting_power_add = 10
		country_electoral_confidence_impact_mult = -0.5
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_institutional_council = {
	group = lawgroup_distribution_of_power
	parent = law_institutional

	icon = "gfx/interface/icons/law_icons/corporate_state.dds"

	progressiveness = 0

	is_visible = {
		has_law = law_type:law_counciliary
	}

	modifier = {
		country_authority_add = 50
		country_legitimacy_govt_total_votes_add = 40
		country_legitimacy_govt_total_clout_add = 80
		country_voting_power_wealth_threshold_add = 5
		country_voting_power_from_literacy_add = 5
		country_voting_power_base_add = 5
		country_peasants_voting_power_add = -5
		country_electoral_confidence_impact_mult = -0.1
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_company_council = {
	group = lawgroup_distribution_of_power
	parent = law_plutocratic

	icon = "gfx/interface/icons/law_icons/interventionism.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_counciliary
	}

	modifier = {
		country_authority_add = 50
		country_legitimacy_govt_total_votes_add = 60
		country_legitimacy_govt_total_clout_add = 60
		country_legitimacy_govt_size_add = 1
		country_aristocrats_voting_power_add = 50
		country_capitalists_voting_power_add = 50
		country_shopkeepers_voting_power_add = 20
		country_bureaucrats_voting_power_add = 20
		country_voting_power_base_add = 50
		country_voting_power_wealth_threshold_add = 20
		country_electoral_confidence_impact_mult = -0.3
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_supervised_participation = {
	group = lawgroup_distribution_of_power
	parent = law_meritocratic

	icon = "gfx/interface/icons/law_icons/census_voting.dds"

	progressiveness = 50

	is_visible = {
		has_law = law_type:law_counciliary
	}

	unlocking_technologies = {
		empiricism
	}

	modifier = {
		country_authority_add = 50
		country_legitimacy_govt_total_votes_add = 80
		country_legitimacy_govt_total_clout_add = 40
		country_legitimacy_govt_size_add = 1
		country_voting_power_wealth_threshold_add = 10
		country_voting_power_from_literacy_add = 50
		country_peasants_voting_power_add = -20
		country_bureaucrats_pol_str_mult = 0.2
		country_officers_pol_str_mult = 0.1
		state_political_strength_from_wealth_mult = 0.1
		country_electoral_confidence_impact_mult = -0.1
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_technocracy
				"ai_ideological_opinion(scope:target_country)" >= 25 # Only want to do this on ideologically similar countries
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_partisan }
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_representative_council = {
	group = lawgroup_distribution_of_power
	parent = law_egalitarian

	icon = "gfx/interface/icons/law_icons/universal_suffrage.dds"

	progressiveness = 100

	is_visible = {
		has_law = law_type:law_counciliary
	}

	unlocking_technologies = {
		egalitarianism
	}

	modifier = {
		country_legitimacy_govt_total_votes_add = 80
		country_legitimacy_govt_total_clout_add = 40
		country_voting_power_wealth_threshold_add = 1
		country_voting_power_from_literacy_add = 10
		country_voting_power_base_add = 50
		country_peasants_voting_power_add = -10
		country_legitimacy_govt_size_add = 2
	}

	can_enact = {
		trigger_if = {
			limit = {
				c:BIC ?= ROOT
				is_subject = yes
				NOT = { has_law = scope:law }
			}
			custom_tooltip = {
				text = BIC_not_able_to_change_distribution_of_power_tt
				has_variable = britain_granted_dop_permission
			}
		}
	}

	on_activate = {
		scripted_effect_parties_disappearence = yes
		cleanup_voting_strength_modifiers = yes
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_anarchist
		}
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			AND = { # modification of the standard trigger, only other anarchic countries can impose anarchy on their subjects
				has_law = scope:law
				can_impose_law_default = yes
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_anarchy
				has_strategy = ai_strategy_egalitarian_agenda
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_egalitarian }
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_party_council = {
	group = lawgroup_distribution_of_power
	parent = law_partisan

	icon = "gfx/interface/icons/law_icons/single_party_state.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_counciliary
	}

	unlocking_technologies = {
		political_agitation
	}

	on_activate = {
		set_only_legal_party_from_ig = ruler.interest_group
		custom_tooltip = {
			text = sps_government_reshuffle_tt
			every_interest_group = {
				limit = {
					is_in_government = yes
					NOT = { #Removes all IGs from government that aren't in your party
						has_party = yes
					}
				}
				remove_ruling_interest_group = yes
			}
			every_interest_group = {
				limit = { #Adds all IGs that are in your party to the government
					is_in_government = no
					has_party = yes
				}
				add_ruling_interest_group = yes
			}
		}

		if = {
			limit = {
				has_law = law_type:law_monarchy
				has_variable = king_in_parliament_possible
				NOT = {
					has_journal_entry = je_king_in_parliament
				}
			}
			custom_tooltip = {
				text = king_in_parliament_tt
				add_journal_entry = { type = je_king_in_parliament }
			}
		}
		if = {
			limit = {
				game_date > 1836.1.1
				country_has_voting_franchise = yes
			}
			initialize_electoral_confidence = yes
		}
	}

	on_deactivate = {
		unset_only_legal_party = yes
		update_party_support = yes
		if = {
			limit = {
				has_variable = fatherland_front_var
			}
			remove_variable = fatherland_front_var
		}
	}

	modifier = {
		country_authority_add = 100
		country_legitimacy_govt_total_clout_add = 60
		country_legitimacy_govt_total_votes_add = 60
		interest_group_in_government_attraction_mult = 0.20
		country_voting_power_wealth_threshold_add = 1
		country_voting_power_from_literacy_add = 10
		country_voting_power_base_add = 30
		country_peasants_voting_power_add = -10
		country_electoral_confidence_impact_mult = -1.0
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			interest_group = {
				has_party = yes
			}
			law_stance = {
				law = law_type:law_partisan
				value > neutral
			}
		}
	}
	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			AND = { # modification of the standard trigger, only other single-party states countries can impose single-party states on their subjects
				has_law = scope:law
				can_impose_law_default = yes
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_partisan
				"ai_ideological_opinion(scope:target_country)" >= 25 # Only want to do this on ideologically similar countries
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_technocracy }
				}
			}

			add = base_impose_law_weight
		}
	}
}

# Parliamentary

REPLACE_OR_CREATE:law_noble_assembly = {
	group = lawgroup_distribution_of_power
	parent = law_aristocratic

	icon = "gfx/interface/icons/law_icons/landed_voting.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_parliamentary
	}

	modifier = {
		country_authority_add = 150
		country_legitimacy_govt_total_votes_add = 50
		country_legitimacy_govt_total_clout_add = 70
		country_legitimacy_govt_size_add = 1
		country_legitimacy_ideological_incoherence_mult = 0.2
		country_aristocrats_voting_power_add = 100
		country_clergymen_voting_power_add = 30
		country_officers_voting_power_add = 30
		country_capitalists_voting_power_add = 20
		country_electoral_confidence_impact_mult = -0.3
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_managed_democracy = {
	group = lawgroup_distribution_of_power
	parent = law_institutional

	icon = "gfx/interface/icons/law_icons/unused/home_affairs.dds"

	progressiveness = 0

	is_visible = {
		has_law = law_type:law_parliamentary
	}

	modifier = {
		country_authority_add = 200
		country_legitimacy_govt_total_votes_add = 60
		country_legitimacy_govt_total_clout_add = 60
		country_legitimacy_govt_size_add = 1
		country_voting_power_wealth_threshold_add = 5
		country_voting_power_from_literacy_add = 10
		country_voting_power_base_add = 30
		country_peasants_voting_power_add = -20
		country_electoral_confidence_impact_mult = -0.9
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE:law_wealth_voting = {
	group = lawgroup_distribution_of_power
	parent = law_plutocratic

	icon = "gfx/interface/icons/law_icons/wealth_voting.dds"

	progressiveness = 0

	is_visible = {
		has_law = law_type:law_parliamentary
	}

	modifier = {
		country_authority_add = 100
		country_legitimacy_govt_total_votes_add = 70
		country_legitimacy_govt_total_clout_add = 50
		country_legitimacy_govt_size_add = 1
		country_legitimacy_ideological_incoherence_mult = 0.2
		country_voting_power_base_add = 50
		country_voting_power_wealth_threshold_add = 20
		state_political_strength_from_wealth_mult = 0.2
		country_electoral_confidence_impact_mult = -0.2
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_literacy_voting = {
	group = lawgroup_distribution_of_power
	parent = law_meritocratic

	icon = "gfx/interface/icons/law_icons/census_voting.dds"

	progressiveness = 50

	is_visible = {
		has_law = law_type:law_parliamentary
	}

	unlocking_technologies = {
		empiricism
	}

	modifier = {
		country_authority_add = 50
		country_legitimacy_govt_total_votes_add = 75
		country_legitimacy_govt_total_clout_add = 45
		country_legitimacy_ideological_incoherence_mult = 0.1
		country_voting_power_wealth_threshold_add = 5
		country_voting_power_from_literacy_add = 70
		country_peasants_voting_power_add = -20
		country_electoral_confidence_impact_mult = -0.1
		country_legitimacy_govt_size_add = 1
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_technocracy
				"ai_ideological_opinion(scope:target_country)" >= 25 # Only want to do this on ideologically similar countries
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_partisan }
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_universal_voting = {
	group = lawgroup_distribution_of_power
	parent = law_egalitarian

	icon = "gfx/interface/icons/law_icons/universal_suffrage.dds"

	progressiveness = 100

	is_visible = {
		has_law = law_type:law_parliamentary
	}

	unlocking_technologies = {
		egalitarianism
	}

	modifier = {
		country_legitimacy_govt_total_votes_add = 90
		country_legitimacy_govt_total_clout_add = 20
		country_voting_power_wealth_threshold_add = 1
		country_voting_power_from_literacy_add = 10
		country_voting_power_base_add = 40
		country_peasants_voting_power_add = -10
		country_legitimacy_govt_size_add = 2
	}

	can_enact = {
		trigger_if = {
			limit = {
				c:BIC ?= ROOT
				is_subject = yes
				NOT = { has_law = scope:law }
			}
			custom_tooltip = {
				text = BIC_not_able_to_change_distribution_of_power_tt
				has_variable = britain_granted_dop_permission
			}
		}
	}

	on_activate = {
		scripted_effect_parties_disappearence = yes
		cleanup_voting_strength_modifiers = yes
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_anarchist
		}
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			AND = { # modification of the standard trigger, only other anarchic countries can impose anarchy on their subjects
				has_law = scope:law
				can_impose_law_default = yes
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_anarchy
				has_strategy = ai_strategy_egalitarian_agenda
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_egalitarian }
				}
			}

			add = base_impose_law_weight
		}
	}
}

REPLACE_OR_CREATE:law_party_assembly = {
	group = lawgroup_distribution_of_power
	parent = law_partisan

	icon = "gfx/interface/icons/law_icons/single_party_state.dds"

	progressiveness = -50

	is_visible = {
		has_law = law_type:law_parliamentary
	}

	unlocking_technologies = {
		political_agitation
	}

	modifier = {
		country_authority_add = 200
		country_legitimacy_govt_total_clout_add = 80
		country_legitimacy_govt_total_votes_add = 30
		interest_group_in_government_attraction_mult = 0.20
		country_voting_power_wealth_threshold_add = 1
		country_voting_power_from_literacy_add = 10
		country_voting_power_base_add = 40
		country_peasants_voting_power_add = -10
		country_electoral_confidence_impact_mult = -1.0
	}

	on_activate = {
		set_only_legal_party_from_ig = ruler.interest_group
		custom_tooltip = {
			text = sps_government_reshuffle_tt
			every_interest_group = {
				limit = {
					is_in_government = yes
					NOT = { #Removes all IGs from government that aren't in your party
						has_party = yes
					}
				}
				remove_ruling_interest_group = yes
			}
			every_interest_group = {
				limit = { #Adds all IGs that are in your party to the government
					is_in_government = no
					has_party = yes
				}
				add_ruling_interest_group = yes
			}
		}
		if = {
			limit = {
				game_date > 1836.1.1
				country_has_voting_franchise = yes
			}
			initialize_electoral_confidence = yes
		}
	}

	on_deactivate = {
		unset_only_legal_party = yes
		update_party_support = yes
		if = {
			limit = {
				has_variable = fatherland_front_var
			}
			remove_variable = fatherland_front_var
		}
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			interest_group = {
				has_party = yes
			}
			law_stance = {
				law = law_type:law_partisan
				value > neutral
			}
		}
	}
	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			AND = { # modification of the standard trigger, only other single-party states countries can impose single-party states on their subjects
				has_law = scope:law
				can_impose_law_default = yes
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
	}
}
