REPLACE:law_autocracy = {
	group = lawgroup_legislative_principle

	icon = "gfx/interface/icons/law_icons/autocracy.dds"

	progressiveness = -100

	modifier = {
		country_authority_add = 300
		country_legitimacy_ideological_incoherence_mult = 0.5
		country_officers_pol_str_mult = 0.5
	}

	on_activate = {
		scripted_effect_parties_disappearence = yes
		cleanup_voting_strength_modifiers = yes
		if = {
			limit = {
				has_law_or_variant = law_type:law_aristocratic
			}
			activate_law = law_type:law_divine_right
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_institutional
			}
			activate_law = law_type:law_loyalism
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_plutocratic
			}
			activate_law = law_type:law_clientelism
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_meritocratic
			}
			activate_law = law_type:law_enlightened_despotate
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_egalitarian
			}
			activate_law = law_type:law_populism
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_personality_rule
			}
			activate_law = law_type:law_partisan
		}
	}
	
	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_autocracy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
				scope:target_country = {
					NOR = { # Is their law different enough from what we want to impose?
						has_law_or_variant = law_type:law_oligarchy
						has_law_or_variant = law_type:law_aristocratic
					}
				}
			}

			add = base_impose_law_weight
		}
	}
}


REPLACE:law_oligarchy = {
	group = lawgroup_legislative_principle

	icon = "gfx/interface/icons/law_icons/oligarchy.dds"

	progressiveness = -50

	modifier = {
		country_authority_add = 200	
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	on_activate = {
		scripted_effect_parties_disappearence = yes
		cleanup_voting_strength_modifiers = yes
		if = {
			limit = {
				has_law_or_variant = law_type:law_aristocratic
			}
			activate_law = law_type:law_aristocracy
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_institutional
			}
			activate_law = law_type:law_institutional_rule
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_plutocratic
			}
			activate_law = law_type:law_plutocracy
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_meritocratic
			}
			activate_law = law_type:law_technocracy
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_egalitarian
			}
			activate_law = law_type:law_anarchy
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_personality_rule
			}
			activate_law = law_type:law_ideological_elite
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_oligarchy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
				scope:target_country = {
					NOR = { # Is their law different enough from what we want to impose?
						has_law_or_variant = law_type:law_autocracy
						has_law_or_variant = law_type:law_aristocratic
					}
				}
			}

			add = base_impose_law_weight
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= ROOT
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

REPLACE_OR_CREATE:law_counciliary = {
	group = lawgroup_legislative_principle

	icon = "gfx/interface/icons/law_icons/council_republic.dds"

	progressiveness = 200

	unlocking_technologies = {
		democracy
	}

	modifier = {
		country_authority_add = 200
		country_legitimacy_ideological_incoherence_mult = -0.1
	}

	on_activate = {
		if = {
			limit = {
				game_date > 1836.1.1
				country_has_voting_franchise = yes
			}
			initialize_electoral_confidence = yes
		}
		scripted_effect_parties_emergence = yes
		inherit_free_elections_effect = yes
		if = {
			limit = {
				has_law_or_variant = law_type:law_aristocratic
			}
			activate_law = law_type:law_noble_council
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_institutional
			}
			activate_law = law_type:law_institutional_council
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_plutocratic
			}
			activate_law = law_type:law_company_council
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_meritocratic
			}
			activate_law = law_type:law_supervised_participation
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_egalitarian
			}
			activate_law = law_type:law_representative_council
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_personality_rule
			}
			activate_law = law_type:law_party_council
		}
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_socialist_ideology = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
	}
}

REPLACE_OR_CREATE:law_parliamentary = {
	group = lawgroup_legislative_principle

	icon = "gfx/interface/icons/law_icons/parliamentary_republic.dds"

	progressiveness = 100

	unlocking_technologies = {
		democracy
	}

	modifier = {
	}

	on_activate = {
		if = {
			limit = {
				game_date > 1836.1.1
				country_has_voting_franchise = yes
			}
			initialize_electoral_confidence = yes
		}
		scripted_effect_parties_emergence = yes
		inherit_free_elections_effect = yes
		if = {
			limit = {
				has_law_or_variant = law_type:law_aristocratic
			}
			activate_law = law_type:law_noble_assembly
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_institutional
			}
			activate_law = law_type:law_managed_democracy
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_plutocratic
			}
			activate_law = law_type:law_wealth_voting
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_meritocratic
			}
			activate_law = law_type:law_literacy_voting
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_egalitarian
			}
			activate_law = law_type:law_universal_voting
		}
		else_if = {
			limit = {
				has_law_or_variant = law_type:law_personality_rule
			}
			activate_law = law_type:law_party_assembly
		}
	}

	ai_will_do = {
		exists = ruler
		OR = {
			ruler = {
				OR = {
					has_ideology = ideology:ideology_republican_leader
					has_ideology = ideology:ideology_radical
					has_ideology = ideology:ideology_positivist
					has_socialist_ideology = yes
				}
			}
			any_interest_group = {
				is_in_government = yes
				is_powerful = yes
				leader = {
					OR = {
						has_ideology = ideology:ideology_republican_leader
						has_ideology = ideology:ideology_radical
						has_ideology = ideology:ideology_positivist
						has_socialist_ideology = yes
					}
				}
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_parliamentary_republic
				has_strategy = ai_strategy_progressive_agenda
				power_bloc ?= { has_identity = identity:identity_ideological_union }
				scope:target_country = { # Is their law different enough from what we want to impose?
					NOT = { has_law_or_variant = law_type:law_presidential_republic }
				}
			}

			add = base_impose_law_weight
			if = {
				limit = {
					scope:target_country = { has_law_or_variant = law_type:law_council_republic }
					NOT = { has_strategy = ai_strategy_egalitarian_agenda }
				}
				multiply = 5
			}
			else = {
				multiply = 0.5 # AI is less likely to mess with governance principles unless dealing with council republics
			}
		}
	}
}